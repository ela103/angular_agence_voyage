import { StatutPaiementPipe } from './statut-paiement-pipe';

describe('StatutPaiementPipe', () => {
  it('create an instance', () => {
    const pipe = new StatutPaiementPipe();
    expect(pipe).toBeTruthy();
  });
});
