import { StatutReservationPipe } from './statut-reservation-pipe';

describe('StatutReservationPipe', () => {
  it('create an instance', () => {
    const pipe = new StatutReservationPipe();
    expect(pipe).toBeTruthy();
  });
});
